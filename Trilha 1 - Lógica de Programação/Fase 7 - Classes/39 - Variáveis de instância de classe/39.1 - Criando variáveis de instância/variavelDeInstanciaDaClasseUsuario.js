// Definição da classe
class Usuario {
    constructor() {
        this.idade = 30;
        this.votou = false;
        console.log(this.idade)
        console.log(this.idade)
    }
}

// Uso da classe
let usuario = new Usuario();
console.log(usuario);