# Operador lógico de negação

- O ponto de exclamação **!** é um operador de negação, ou seja:
    - !true -> false
    - !false -> true

```
let idade = 20
!(idade >= 19)  // false
```

## Exercício - Inverter booleano
Complete a função inverterBooleano() para que ela funcione assim:

Quando receber true como parâmetro, deve retornar false.
Quando receber false como parâmetro, deve retornar true.
Tente resolver este desafio sem usar uma condição if.

### Testes
- Implemente a função inverterBooleano().

### Código

- [Inverter booleano](./inverterBooleano.js)