let curso = {
  nome: "DEVstart",
  descricao: "O DEVstart forma desenvolvedores front-end júnior",
  estaCompleto: true,
};

// estava assim antes
//console.log(curso.descrica.toUpperCase()); 

console.log(curso.descricao.toUpperCase()); 
